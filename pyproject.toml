[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "ai-software-factory"
version = "0.1.0"
description = "Technical foundation for the AI Software Factory framework."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "AI Software Factory contributors" }]
dependencies = [
    "gitpython>=3.1.43",
    "pydantic>=2.8.0",
    "pyyaml>=6.0.2",
    "typer>=0.12.3",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.10.0",
    "pytest>=8.2.0",
    "ruff>=0.5.0",
]
crewai = [
    # CrewAIRuntime targets the public Agent/Task/Crew/Process APIs available in 0.121.x.
    "crewai>=0.121.0,<1.0.0",
]

[project.scripts]
factory = "ai_software_factory.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_software_factory"]

[tool.hatch.build.targets.wheel.force-include]
"src/ai_software_factory/resources" = "ai_software_factory/resources"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"
markers = [
    "crewai_integration: optional CrewAI integration tests requiring external credentials",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ANN"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
packages = ["ai_software_factory"]

[[tool.mypy.overrides]]
module = ["git", "git.*", "typer", "typer.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "ai_software_factory.cli.app"
disallow_untyped_decorators = false

[tool.ruff.lint.per-file-ignores]
"src/pydantic.py" = ["ANN", "B007"]
"src/typer/**" = ["ANN"]
"src/git.py" = ["ANN"]
